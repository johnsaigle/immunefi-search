source 'https://rubygems.org'

ruby '>= 3.0.0'

# No external dependencies required - using only Ruby standard library
# All functionality implemented with:
# - net/http for HTTP requests
# - json for JSON parsing  
# - fileutils for file operations
# - optparse for command line parsing
# - set for collections

group :development, :test do
  # Add testing gems here if needed in the future
  # gem 'rspec'
  # gem 'rubocop'
end